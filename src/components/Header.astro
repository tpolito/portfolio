---
import { divider } from 'styled-system/patterns';
import { css } from 'styled-system/css';
interface Props {
	title?: string;
	subtitle?: string;
}

const {
	title = "Hello there, I'm Tyler 👋",
	subtitle = 'I am a software enginneer and game developer who loves to write about software engineering, games, and other hobbies.'
} = Astro.props;
---

<h1
	class={css({
		color: 'textLight',
		fontSize: '3rem',
		fontWeight: 700,
		fontFamily: 'roboto',
		lineHeight: 1.2,
		margin: 0,
		padding: 0
	})}
>
	{title}
</h1>
<div>
	<p
		class={css({
			color: 'textLight',
			fontSize: '1.5rem',
			fontFamily: 'roboto'
		})}
	>
		{subtitle}
	</p>
</div>
<div class={divider({ color: 'purple', my: '2rem' })}></div>
